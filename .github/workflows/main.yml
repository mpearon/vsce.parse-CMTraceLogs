name: test-workflow

on:
  push:
    branches: 
      - main

jobs:
  call-workflow-passing-data:
    uses: mpearon/private-workflows/.github/workflows/vsce-extension-BuildPublish.yml@v1
    secrets:
      VSCE_TOKEN: ${{ secrets.VSCE_TOKEN }}
  
