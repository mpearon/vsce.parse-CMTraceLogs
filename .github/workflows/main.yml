name: test-workflow

on:
  push:
    branches: 
      - main

jobs:
  call-workflow-passing-data:
    uses: mpearon/private-workflows/.github/workflows/vsce-extension-BuildPublish.yml@main
    secrets:
      VSCE_TOKEN: ${{ secrets.VSCE_TOKEN }}
  
